import { GameplayHistoryFilterPipe } from './gameplay-history-filter.pipe';

describe('GameplayHistoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameplayHistoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
