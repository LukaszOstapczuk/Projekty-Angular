import { SortHighscorePipe } from './sort-highscore.pipe';

describe('SortHighscorePipe', () => {
  it('create an instance', () => {
    const pipe = new SortHighscorePipe();
    expect(pipe).toBeTruthy();
  });
});
